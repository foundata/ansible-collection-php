# Main entry point for the role.

---

- name: "Main | Include role initialization tasks"
  ansible.builtin.include_tasks:
    file: "{{ role_path }}/tasks/init.yml"
    apply:
      tags:
        - "always"
        - "run_php_always"
  tags:
    - "always"
    - "run_php_always"


# Separate includes for platform-specific files and default.yml on purpose. When
# looping over "platform files + ['default.yml']" for multiple platforms in one
# play, Ansible batches them across all hosts. Insertion order depends on when
# results arrive (non-deterministic behavior) and default.yml may run before the
# platform-specific files. Separate tasks enforce the required barrier.

- name: "Main | Include setup tasks (install): best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_setup"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/setup/install/{{ __run_php_loop_item }}"
  when:
    - run_php_state == 'present'
    - __run_php_task_file is ansible.builtin.file
  loop: "{{ __run_php_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__run_php_loop_item"
  tags:
    - "run_php_setup"


- name: "Main | Include setup tasks (install): default"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_setup"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/setup/install/default.yml"
  when:
    - run_php_state == 'present'
    - __run_php_task_file is ansible.builtin.file
  tags:
    - "run_php_setup"


- name: "Main | Include configuration tasks: best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_config"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/config/{{ __run_php_loop_item }}"
  when:
    - run_php_state == 'present'
    - __run_php_task_file is ansible.builtin.file
  loop: "{{ __run_php_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__run_php_loop_item"
  tags:
    - "run_php_config"


- name: "Main | Include configuration tasks: default"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_config"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/config/default.yml"
  when:
    - run_php_state == 'present'
    - __run_php_task_file is ansible.builtin.file
  tags:
    - "run_php_config"


- name: "Main | Include service or daemon management tasks: best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_service"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/service/{{ __run_php_loop_item }}"
  when:
    - run_php_state == 'present'
    - __run_php_task_file is ansible.builtin.file
  loop: "{{ __run_php_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__run_php_loop_item"
  tags:
    - "run_php_service"


- name: "Main | Include service or daemon management tasks: default"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_service"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/service/default.yml"
  when:
    - run_php_state == 'present'
    - __run_php_task_file is ansible.builtin.file
  tags:
    - "run_php_service"


- name: "Main | Include setup tasks (uninstall): best platform matches (if any)"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_setup"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/setup/uninstall/{{ __run_php_loop_item }}"
  when:
    - run_php_state == 'absent'
    - __run_php_task_file is ansible.builtin.file
  loop: "{{ __run_php_platform_filenames_most_specific_first }}"
  loop_control:
    loop_var: "__run_php_loop_item"
  tags:
    - "run_php_setup"

- name: "Main | Include setup tasks (uninstall): default"
  ansible.builtin.include_tasks:
    file: "{{ __run_php_task_file }}"
    apply:
      tags:
        - "run_php_setup"
  vars:
    __run_php_task_file: "{{ role_path }}/tasks/setup/uninstall/default.yml"
  when:
    - run_php_state == 'absent'
    - __run_php_task_file is ansible.builtin.file
  tags:
    - "run_php_setup"
