# Internal role variables specific to: Debian 13 (distribution_major_version)
#
# Only values specific to the platform and its functionality should be included
# here. Variables in this file override those defined in vars/debian.yml.

---

# Debian 13 (Trixie) ships PHP 8.4 as the default in main repos.
__run_php_version_distribution_default: "8.4"
